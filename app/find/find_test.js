'use strict';

describe('myApp.find module', function () {

  beforeEach(module('myApp.find'));

  describe('find controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var findCtrl = $controller('findCtrl');
      expect(findCtrl).toBeDefined();
    }));

  });
});